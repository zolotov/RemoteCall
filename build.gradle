version = 1.6

apply plugin: 'java'

repositories {
	flatDir(dirs: file(path_to_idea + "/lib/"))
	mavenCentral()
}

dependencies {
	compile module(':idea') {
		dependency(':openapi')
		dependency(':util')
		dependency(':annotations')
	}

	testCompile group: 'org.testng', name: 'testng', version: '5.14+'
    compile 'com.google.guava:guava:14+'
}

test {
	useTestNG()
}
